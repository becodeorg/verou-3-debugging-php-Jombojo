// if (gettype($int) === gettype(var_dump($_GET["money"])) || gettype($double) === gettype(var_dump($_GET["money"]))) {
            // var_dump($_GET("money"))
        // } else {
            // echo "error";
        // }